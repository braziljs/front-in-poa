---
layout: default
---

<% for section in @sections : %>
  <!-- <%= section.toUpperCase() %> -->

  <section class="section-<%= section %>" id="<%= section %>">
    <%- @partial("section/#{section}.html.coffee", @) %>
  </section>

  <!-- / <%= section.toUpperCase() %> -->
<% end %>
